/**
 * Site-wide configuration.
 *
 * SIGNUP_URL is where every "Start free" / "Start building free" CTA points.
 * Change it in one place to update them all.
 */
export const SIGNUP_URL = 'https://app.pagistry.com/';
